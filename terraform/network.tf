# No networking resources required for the Free (F1) App Service plan.
# VNet integration (and associated private endpoints, subnets, and DNS zones)
# is only available on Basic (B2+) and higher plans.
#
# To re-enable private networking, upgrade app_service_sku to P1v3 or higher
# and restore VNet integration, private endpoints, and DNS zones.
